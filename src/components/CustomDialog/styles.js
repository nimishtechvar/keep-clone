import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  dialogContainer: {
    minWidth: "600px",
    minHeight: "auto",
    padding: "20px",
    marginBottom: "0",
  },
  dialog: {
    position: "absolute",
    left: 300,
    top: 80,
  },
});

export default useStyles;
