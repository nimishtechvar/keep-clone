import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f5ba13",
    },
    background: {
      default: "#DFDFDF",
      paper: "#FFFFFF",
    },
    warning: {
      main: "#f50f07",
    },
  },
});
